apply {
    plugin 'maven-publish'
}

java {
    withSourcesJar()
}

publishing {
    repositories {
        maven {
            name project.name
            url maven_github_repo + project.name
            credentials {
                username System.getProperty('github.username')
                password System.getProperty('github.token')
            }
        }
    }
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
